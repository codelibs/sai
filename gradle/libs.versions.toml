# Dependency coordinates for the Sai build.
#
# Bump a version here and every use of it follows. `./gradlew dependencyUpdates` is not
# configured; check https://central.sonatype.com for newer releases.

[versions]
# ASM is the bytecode backend for both the code generator and saigen.
asm = "9.10.1"
# TestNG 7 needs Java 11+, which the toolchain already guarantees. jcommander, bsh and
# snakeyaml used to be declared next to it because the Ant build assembled the test classpath
# by hand; nothing in the tree references them, and TestNG now brings what it needs itself.
testng = "7.12.0"

[libraries]
asm-core = { module = "org.ow2.asm:asm", version.ref = "asm" }
asm-analysis = { module = "org.ow2.asm:asm-analysis", version.ref = "asm" }
asm-commons = { module = "org.ow2.asm:asm-commons", version.ref = "asm" }
asm-tree = { module = "org.ow2.asm:asm-tree", version.ref = "asm" }
asm-util = { module = "org.ow2.asm:asm-util", version.ref = "asm" }

testng = { module = "org.testng:testng", version.ref = "testng" }

[bundles]
asm = ["asm-core", "asm-analysis", "asm-commons", "asm-tree", "asm-util"]
